\usepackage[style=ieee,
            citestyle=numeric-comp,  % Use [2-5] compression
            backend=biber]{biblatex}
\addbibresource{references.bib}      % path is relative to main.tex, not here

\setlength{\bibitemsep}{0.5em} % '1.5ex' is a nice "little bit" of space
